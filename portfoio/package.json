{
	"name": "portfoio",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"start": "vite",
		"bundle": "npm run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "^3000.1.17"
	},
	"devDependencies": {
		"vite": "^5.3.1"
	}
}
